from pyspark_k8s_boilerplate.config.handlers import GeneralConfig, \
    DataConfig, ModelConfig

cfg: GeneralConfig = GeneralConfig()

data_cfg: DataConfig = DataConfig()

model_cfg: ModelConfig = ModelConfig()
